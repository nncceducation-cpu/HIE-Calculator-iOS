{
  "name": "hie-calculator",
  "version": "1.0.0",
  "private": true,
  "description": "HIE Calculator — therapeutic hypothermia eligibility for neonatal HIE",
  "scripts": {
    "build": "echo \"static web app in www/ — nothing to compile\"",
    "cap:add:ios": "cap add ios",
    "cap:sync": "cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
